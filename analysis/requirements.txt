# Install with user-site packages to avoid system "externally-managed-environment" errors:
#   python3 -m pip install --user --break-system-packages -r analysis/requirements.txt
# Or use: ./analysis/run_plotter.sh results -o analysis/plots --no-show
matplotlib>=3.7.0
